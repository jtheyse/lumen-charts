# Verification record

Final checks: 10 September 2026. Windows x64; .NET SDK 10.0.400. Reusable packages target net8.0; gallery and executable tests target net10.0.

## Automated results

- Release solution build: passed, zero warnings and zero errors.
- Executable library/component regression suite: **131 passed, 0 failed**.
- HTTP integration script against the running gallery: **57 passed, 0 failed**.
- Browser suite driving a headless Chromium: **13 passed, 0 failed against the server gallery** and **13 passed, 0 failed against the WebAssembly host**. It covers tooltips on hover and on keyboard focus, Escape dismissal, point selection, SVG, CSV and PNG downloads verified by their bytes, series hiding, the data table, zoom and reset, and graph node dragging, selection and layout reset.
- Local NuGet installation smoke test: restored all three 0.6.1 packages from `artifacts/packages`, compiled a consuming application, rendered SVG using the installed core package, and loaded the Blazor component and endpoint-extension types successfully.
- Package contents include library binaries and the Blazor static assets. Research files and extraction scratch files are excluded.

The regression suite covers all ten chart families in normal and empty states, scales, zero baselines, negative stacks, missing observations, SVG escaping, color validation, invalid bounds, numeric culture, sampling, CSV safety/preservation, graph cycles/endpoints/determinism and Blazor static prerender. Seventeen of those checks cover the 0.2.0 axes: calendar tick alignment at hour, day, month and year ranges, invariant UTC labels, decade and 2/5 log ticks, log mapping and inversion, the ISO timestamp CSV column, and rejection of nonpositive log data, time axes on Y, log axes on magnitude or radial charts, unknown axis kinds and out-of-range time values. Ten checks cover the 0.6.0 accessibility pass: the contrast helper is validated against published WCAG ratios, every palette and candlestick color clears 3:1 on both backgrounds, every text color clears 4.5:1, graph edges clear 3:1, every mark in all fourteen kinds carries a name and a supported role, no element takes a positive tab index, graph nodes are named focusable buttons, heatmap cells carry a delineating stroke, the component exposes its live region and labelled controls, and the published libraries reference nothing host-specific. Ten checks cover the 0.5.0 graph work: an avoidable crossing is removed while a graph that needs no reordering keeps its input order, long edges bend once per level and clear the node between them, routes and crossing counts repeat exactly, self-loops keep both endpoints on their node, circular crossings count interleaved chords, rendered nodes carry drag handles and positions, overridden positions move a node and the edge endpoints that reach it, layered graphs accept self-loops but still reject longer cycles, and the graph component prerenders its nodes and reset control. Four checks cover the 0.4.0 export path: marks drop and keep native titles on request, aggregates honor the same switch, and exported SVG stays self-contained (a viewBox, an explicit background, no external references, images or foreign objects) so a canvas can rasterize it. Twenty-nine further checks cover the 0.3.0 families: quantile interpolation against hand-computed values, Tukey fences and outlier selection, bin coverage and counts, degenerate and automatic bin selection, candlestick wick and body geometry with directional colors, candlesticks on time and log axes, band fills split by missing bounds, histogram bar heights against the computed counts, box quartiles with indexed outliers and per-series glyphs, family CSV columns, and rejection of incomplete candles, half-specified bands, multiple candlestick or histogram series and unsupported axis combinations. HTTP checks cover assets, SVG/CSV endpoints, time and log specifications, all fourteen chart kinds, the four new families with their own geometry markers, graph coordinates and invalid requests.

PNG rasterization and tooltip behavior are browser features: they are verified in the browser section below, not by the executable suite. Reproduce with the commands in README and `./tests/verify-api.ps1` while the gallery is running. These are actual executable assertions, not a `dotnet test` discovery-based suite.

## Browser checks

Verified in the Codex in-app browser:

- All ten chart selectors became active and produced marks: 36 each for line/area/scatter/bubble/column/bar/stacked, 4 donut slices, 84 heatmap cells and 12 radar points for the sample datasets.
- Keyboard Enter on a donut slice selected the original observation and updated the status and parent callback display.
- View data showed the original four-row donut dataset.
- Hiding Workspace reduced visible marks from 36 to 24 and changed its pressed state; restoring it brought the series back.
- Zoom narrowed displayed months, pan shifted the interval, and reset restored the original range.
- Theme switching changed the gallery and SVG appearance. Circular layout replaced the layered network drawing.
- WebAssembly (10 September 2026): `samples/Lumen.Wasm` served on http://localhost:5199 returned 200 for the host page and for both class-library static assets. The chart rendered 36 marks and the graph 7 nodes with the full toolbar. Hovering a mark showed "Workspace: Jan, 37.2"; clicking it set the component status and the host callback to "Selected series 1, observation 1"; Export PNG reported "Exported chart.png"; the candlestick, histogram, box and donut samples each rendered their own marks (30, 16, 7 and 4); and dragging the Transform node reported "Moved Transform" with a new stored position. This is the same behavior as the Interactive Server gallery.
- Accessibility (10 September 2026): the gallery's accessibility tree exposes each mark as a named button ("Workspace: Sep, 60.3"), the chart as a group named "A clearer view of growth. Monthly activity across three product plans", and graph nodes as named buttons. Heading order runs h1, h2, h3 with main, aside, header and footer landmarks; no control lacks a name; no positive tab index appears; the data table carries a caption and `scope="col"` headers, and its toggle reports `aria-expanded`. Heatmap cells render with the new hairline. This is browser-tree evidence, not a screen-reader or audit-tool result.
- Graph interaction (10 September 2026): dragging the Transform node previewed with a live transform and committed on release, moving it from 522,255 to 578,171, setting the status to "Moved Transform" and pulling its edge endpoints with it. ArrowUp on a focused node moved it eight units, a press without movement raised NodeSelected and updated the gallery hint, and Reset layout restored 522,255. The demonstration graph reported "7 nodes · 8 directed connections · Layered layout · 0 edge crossings" and drew its level-spanning edge as a single curved path.
- Exports and tooltips (10 September 2026): hovering a line mark showed the HTML tooltip "Workspace: Sep, 60.3" anchored above it, and the interactive chart carried one `<title>` (the chart name) with none on its marks, while the network graph kept its own native titles. Pressing Export PNG set the toolbar status to "Exported chart.png", meaning the browser resolved the rasterization promise. A direct check of the same pipeline produced an untainted 1800x840 canvas with about 100,600 non-background pixels and a 142 KB PNG blob at the 2x scale.
- Families (10 September 2026): the candlestick demo drew 30 daily candles with teal rising and pink falling bodies, weekly date ticks and an `open / high / low / close` tooltip; the band demo drew a widening interval around its projection; the histogram binned 320 sampled requests into 22 equal-width bins with a zero baseline; the box demo drew three regional distributions with quartile boxes, whiskers, outlier circles and observation counts. A first run showed epoch numbers on the candlestick X axis because the demo data set timestamps without selecting the time axis, and the histogram bin summary overlapped the source line; both were corrected and re-verified.
- Axis switch (10 September 2026): Time X redrew the area chart with fortnightly ticks from 5 Jan to 16 Mar 2026 and date tooltips ("Workspace: 5 Jan 2026, 47"); zooming narrowed it to 26 Jan – 2 Mar with weekly ticks. Log Y on the line chart produced decade ticks 10, 100, 1000, 10000. The Log Y button is hidden for area, column, bar, stacked, donut, heatmap and radar, matching the validation rules.
- The browser's download-event waiter timed out, but the actual exported files were subsequently found in Downloads: CSV contained 36 original observations, and SVG parsed as valid XML. The event-tool timeout was not treated as an application failure.
- A 390-pixel viewport exposed overly small labels in the initial proportional layout. The final component uses a focusable horizontal scrolling viewport; the final mobile screenshot confirmed the scrollbar and larger labels without page-wide horizontal overflow. Desktop viewport override was restored afterward.

## Issues found and corrected

- The first browser-automated run failed on hover with `<svg> intercepts pointer events`. Marks on the first and last value sat exactly on the plot's clipping boundary, so half of each mark was clipped away and a pointer at its own centre landed on the boundary and missed. The clipping viewport is now inset by one marker radius, which also caps how far an out-of-range value can bleed when zoomed. Two of that run's three failures were faults in the new tests rather than the product: a drag aimed at a node group's centre, which falls in the gap between the circle and its label, and a mouse drag at viewport coordinates without scrolling the target into view first.

- The 0.6.0 contrast measurement failed three palette colors against the light background: `#18A999` at 2.93, `#DA9650` at 2.48 and `#58A4C1` at 2.80, all below the 3:1 needed for non-text contrast. They were replaced with `#169B8D`, `#B87F44` and `#4F93AD`, which measure 3.44, 3.41 and 3.44 on light and stay above 4.8 on dark. The candlestick rising color followed the palette. The palest heatmap cells measured 1.18 against a white background and now carry a hairline outline.
- A Blazor WebAssembly host failed to build with `NETSDK1082`, hunting for an ASP.NET Core runtime pack for `browser-wasm`. A stock `dotnet new blazorwasm` project built cleanly on the same machine, which located the cause in this repository: `Lumen.Charts.Blazor` declared `<FrameworkReference Include="Microsoft.AspNetCore.App"/>`, requiring the ASP.NET Core shared framework that a WebAssembly host does not have. The component library was therefore unusable in WebAssembly, not merely untested. It now references the `Microsoft.AspNetCore.Components.Web` package, the sample builds and runs, and the repository restores that one package from nuget.org.

- Rewriting the layered layout changed one documented behavior: a self-loop no longer counts towards the cycle check, so a layered graph containing one is drawn instead of rejected. The HTTP script caught the change, the loop renders as a curve on its node, and the README and tests now state the rule.

- A missing Razor event namespace prevented initial component event binding.
- Gallery static-asset routing initially returned 404 for the framework script and RCL resources, leaving the page noninteractive. Corrected with .NET 10 asset mapping and a Development launch profile; assets now return 200 and browser interactions work.
- SVG accessibility grouping initially hid individual marks from the accessibility tree. Changed grouping; point buttons became visible to the browser accessibility tools.
- Bubble size normalization was moved out of the per-point loop.
- Added null-name validation and preserved readable chart size on narrow screens.

## Practical limits

A diagnostic run generated an SVG from 100,000 line observations in approximately 60 ms with at most 1,200 rendered sample points on this machine. This measures SVG generation only, not network time, browser layout, interaction latency or memory, and is not a comparison with another library.

WebAssembly hosting is exercised by the sample above but not under production load or on other browsers. Screen-reader conformance, production load, all operating systems, touch interaction, and very dense graphs have not been certified. Very large scatter/bubble inputs render every point. Dense graph layouts and extreme chart proportions still need application-level review. See README and RESEARCH.md for the exact scope and future capabilities.
